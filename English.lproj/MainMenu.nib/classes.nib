{
    IBClasses = (
        {
            ACTIONS = {makeDonation = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {searchResult = id; searchResultController = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}